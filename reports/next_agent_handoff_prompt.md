# Next Agent Handoff Prompt

Use this prompt for the next implementation agent.

```text
You are working in /home/naki/Desktop/itsthatnewshit/FlashRL.

Operating contract:
- First read /home/naki/tools/universal-ml-intern/AGENTS.md and follow it.
- Then read the repo artifacts generated by the prior audit:
  - reports/flashrl_repo_audit.md
  - reports/flashrl_research_review.md
  - reports/flashrl_v2_implementation_plan.md
  - docs/environment_api.md
  - docs/benchmark_protocol.md
  - docs/game_expansion_plan.md
  - issues/backlog.md
- Treat those files as the current technical spec. They are based on inspecting actual code, not just README claims.

High-level objective:
Turn FlashRL from a fragile Chrome Dino DQN demo into a serious, reproducible RL benchmark for browser games.

Critical current findings to preserve:
- DinoEnv currently returns structured state `[trex_y, obstacle_x, obstacle_width]`, not pixels.
- dqn_train.py and dqn_eval.py incorrectly feed that structured state through image preprocessing into an Atari-style CNN.
- README.md and REPORT.md contain many stale or false claims: non-existent commands, non-existent project structure, TensorFlow examples in a PyTorch repo, unverified score claims, and unsupported pixel-DQN claims.
- requirements.txt installs Gymnasium, but dino_env.py imports legacy Gym. Clean-clone setup is not reproducible.
- Current algorithm is vanilla DQN with uniform replay and a target network. It is not Double DQN, Dueling DQN, PER, N-step, NoisyNet, C51/QR-DQN, Rainbow, PPO, or recurrent PPO.
- Existing checkpoints do not contain enough metadata to prove reported performance.

Implementation order:
1. Phase 0 repo cleanup and reproducibility.
2. Phase 1 Gymnasium migration and environment correctness.
3. Phase 2 random/rule-based baselines and benchmark harness.
4. Phase 3 DQN cleanup, Double DQN, Dueling DQN, PER/N-step, PPO comparison.
5. Phase 4 state/vision/hybrid comparison.
6. Phase 5 additional browser games.
7. Phase 6 dashboard, videos, README polish.

Start with Phase 0 and Phase 1 only unless explicitly asked to go further.

Concrete first tasks:
- Update README.md so quick-start commands match actual files or new entrypoints.
- Fix dependencies and setup instructions, including Playwright browser install.
- Migrate dino_env.py from Gym to Gymnasium:
  - `import gymnasium as gym`
  - `reset(seed=None, options=None) -> (obs, info)`
  - `step(action) -> (obs, reward, terminated, truncated, info)`
  - add `metadata`, `render_mode`, `max_episode_steps`, idempotent close
- Add explicit observation modes:
  - `state`: normalized structured vector
  - `vision`: real screenshot/canvas pixels
  - `hybrid`: dict with image and state
- Stop passing structured vectors through image preprocessing.
- Add separate MLP and CNN networks matched to observation mode.
- Add smoke tests and Gymnasium env-check tests.

Validation expectations:
- Run static import/compile checks.
- Run at least one no-training environment smoke test.
- If browser/Playwright setup blocks execution, document the exact blocker and add a deterministic non-browser unit test path.
- Do not claim performance until benchmark result files exist.

Repo hygiene constraints:
- Do not commit `.env`, `__pycache__/`, `session_logs/`, debug frames, or heavyweight checkpoints.
- Keep changes small and phase-scoped.
- Do not delete user work unless explicitly requested.
- Commit only coherent artifacts/implementation changes.

Tone:
Be blunt and evidence-based. Remove misleading claims rather than polishing them.
```

